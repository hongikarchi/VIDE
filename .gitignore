# OS / editor
Thumbs.db
Desktop.ini
.DS_Store
*.swp
*~

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# private visual references (site / legal data) — keep out of the repo
docs/assets/reference/*.html

# build / dependencies (later)
node_modules/
dist/
build/
bin/
obj/
*.log
